<p id="notice"><%= notice %></p>

<h1>Purchase Orders</h1>

<table>
  <thead>
    <tr>
      <th>Client</th>
      <th>Latitude</th>
      <th>Longitude</th>
      <th>Total</th>
      <th>Proveedor</th>
      <th>Products</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @purchase_orders.each do |purchase_order| %>
      <tr>
        <td><%= purchase_order.client %></td>
        <td><%= purchase_order.latitude %></td>
        <td><%= purchase_order.longitude %></td>
        <td><%= purchase_order.total %></td>
        <td><%= purchase_order.proveedor %></td>
        <td><%= purchase_order.products %></td>
        <td><%= link_to 'Show', purchase_order %></td>
        <td><%= link_to 'Edit', edit_purchase_order_path(purchase_order) %></td>
        <td><%= link_to 'Destroy', purchase_order, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Purchase Order', new_purchase_order_path %>
